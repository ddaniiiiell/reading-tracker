# reading-tracker

## Development note

Before making or pushing changes, check `git status` and make sure any local app changes or recovered browser `localStorage` data that should persist are committed and pushed to the repo.
